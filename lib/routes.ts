/**
 * Primary CTA destination for the site.
 *
 * All site-wide "Get Started" and "Book a Free Call" buttons should link to
 * GET_STARTED_PATH so signup traffic lands on the qualification form.
 */
export const GET_STARTED_PATH = "/get-started" as const;
